import 'package:flutter/material.dart';

class Notificaitions extends StatelessWidget {
  const Notificaitions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
