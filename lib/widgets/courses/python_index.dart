import 'package:flutter/material.dart';

class PythonIndex extends StatelessWidget {
  const PythonIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
